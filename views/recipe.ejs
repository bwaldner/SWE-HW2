<!DOCTYPE html>
<html>
<head>
    <title><%= recipe.RecipeName %></title>
    <link rel="stylesheet" href="/recipe.css">
</head>
<body>
    <div class="container">
        <a href="/recipes">← Back to Recipes</a>
        <div class="recipe-card">
            <h3><%= recipe.RecipeName %></h3>
            <p><strong>Protein:</strong> <%= recipe.Protein %></p>
            <p><strong>Prep Time:</strong> <%= recipe.PrepTime %> hrs</p>
            <p><strong>Cook Time:</strong> <%= recipe.CookTime %> hrs</p>
            <p><strong>Servings:</strong> <%= recipe.Servings %></p>

            <h4>Ingredients:</h4>
            <ul>
                <% ingredients.forEach(ingredient => { %>
                    <li><%= ingredient.IngredientName %>: <%= ingredient.Info %></li>
                <% }) %>
            </ul>
        </div>
    </div>
</body>
</html>
